name: eljur_auth_util
description: CLI utility for authenticating in Eljur via Gosuslugi (ESIA).
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  args: ^2.6.0
  http: ^1.6.0
  interact: ^2.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
